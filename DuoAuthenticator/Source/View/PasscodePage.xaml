<Core:Page
    x:Class="DuoAuthenticator.View.PasscodePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Core="using:Venz.UI.Xaml">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox
            x:Name="Code"
            IsReadOnly="True"
            Margin="10"
            FontSize="64"
            Text="{Binding Passcode}">
        </TextBox>
        <ProgressBar
            x:Name="AutoRefreshIndicator"
            Grid.Row="1"
            Margin="10,0"
            Height="10"
            Minimum="0"
            Maximum="15"
            Value="{Binding RefreshIndicatorProgress}">
        </ProgressBar>
        <Grid Margin="10,0" Grid.Row="2">
            <CheckBox Margin="0,-100,0,0" Content="Auto close application"/>
            <Button Width="170" Content="Export" HorizontalAlignment="Left" Tapped="OnExportTapped"/>
        </Grid>
    </Grid>
</Core:Page>
